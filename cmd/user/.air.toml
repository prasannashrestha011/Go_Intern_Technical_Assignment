# .air.toml inside cmd/auth
root = "../../"
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp/user.exe ."  # <-- point to current folder
bin = "tmp/user.exe"                # <-- must include .exe
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["tmp", "vendor"]

[log]
time = true
